# ROCmForge backend requirements (Phase 1 baseline)
# Install on the MI300X ROCm instance (or inside the ROCm/vLLM container)
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.9.0
python-multipart>=0.0.12

# For later phases (agents, vLLM client, etc.) — commented until Phase 2
# langgraph>=0.2.0
# openai>=1.50.0   # for vLLM OpenAI-compatible client
# httpx>=0.27.0
