# Point this to your backend (local or the MI300X instance)
NEXT_PUBLIC_API_URL=http://localhost:8000

# When running on the real hardware, set to the public IP or domain of your MI300X instance + port
# NEXT_PUBLIC_API_URL=http://your-mi300x-ip:8000